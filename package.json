{
  "name": "webpack-output-transform-plugin",
  "version": "1.0.0",
  "description": "A plugin to transform output files",
  "main": "./dist/build.js",
  "scripts": {
    "build": "mkdir -p ./dist && touch ./dist/build.js && babel ./src/index.js --out-file ./dist/build.js",
    "test": "jest && jest-coverage-badges"
  },
  "author": "davidwoodsandersen",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^23.6.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.6.0",
    "jest-cli": "^23.6.0",
    "jest-coverage-badges": "^1.0.0",
    "jest-junit": "^5.2.0"
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": [
      "json-summary",
      "html"
    ]
  }
}
